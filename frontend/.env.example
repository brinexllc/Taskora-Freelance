TASKORA_ENV=local
API_URL=http://127.0.0.1:8000/api
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api
# At a trusted HTTPS ingress, set the exact public frontend host. The ingress
# must overwrite forwarded headers; do not expose the Node listener publicly.
# VINEXT_TRUSTED_HOSTS=taskora.example
# RELEASE_SHA=<deployed-commit>
